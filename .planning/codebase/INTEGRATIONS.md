# Integrations

## external APIs & Services
- None explicitly defined as external HTTP dependencies within the root configuration yet. 

## Databases
- **MariaDB**: Relational database used for persistence. Connected via `org.mariadb.jdbc.Driver`.

## Security & Authentication Providers
- **Spring Security**: Internal handling of authentication and authorization.

## Webhooks & Events
- Not currently documented or visible in core dependencies.
